{
  "dependencies": {
    "@iptv/playlist": "^0.0.4",
    "iptv-checker": "^0.28.0"
  },
  "scripts": {
	"build": "npm run list && npm run check",
	"list": "node index.js > damn.m3u",
	"check": "iptv-checker damn.m3u -o lista",
	"test": "echo \"Error: no test specified\""
  }
}
